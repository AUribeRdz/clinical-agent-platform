{
  "name": "clinical-agent-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for Medable eTMF demo — wire to Claude Desktop",
  "type": "module",
  "scripts": {
    "start": "node src/etmf-server.js",
    "dev": "node --watch src/etmf-server.js",
    "build": "tsc"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
